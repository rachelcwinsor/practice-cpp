#include <iostream>

int main(){
  std::cout << "Hello world!" << std::endl;
  /*
cout is standard output; global object that represents the standard output as a string
std -> cout is not directly available, it is in a namespace. you can add any class to any namespace
   */
  return 0;
}
